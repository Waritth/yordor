// @yordor/engine — pure scoring logic (no I/O, deterministic).
// Implemented in P1 per docs/02_SCORING_LOGIC_SPEC.md:
//   net(), bonusMult(), turboMult(), teamRanked(), computeTeam(), ...
// Placeholder export keeps the package valid until P1.

export const ENGINE_VERSION = "0.1.0";
